#!/bin/bash

set -e

if [ -z "$wpub" ]; then
    echo "Undefined or empty wpub environment variable!"
    exit 1
fi

if [ -z "$pgservice_core" ]; then
    echo "Undefined or empty pgservice_core!"
    exit 1
fi


$wpub/wsh.php --api=fdl_deletefamily --famid=FORUM
RET=$?
if [ $RET -ne 0 ]; then
    echo "Error updating class Style."
    exit $RET
fi

exit 0
