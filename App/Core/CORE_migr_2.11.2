#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi

echo "DELETE FROM sessions" | PGSERVICE="$pgservice_core" psql
echo "ALTER TABLE sessions ADD COLUMN name text NOT NULL" | PGSERVICE="$pgservice_core" psql