#!/bin/bash

if [ "$dbpsql" == "" ]; then
  . /etc/freedom.conf
   wchoose -b
fi

/usr/bin/php -q $wpub/API/updateclass.php  --class=ParamDef


echo "update paramdef set isglob='N';update paramdef set isglob='Y' from paramv where paramv.name=paramdef.name and paramv.type='G';" | psql $dbpsql;
