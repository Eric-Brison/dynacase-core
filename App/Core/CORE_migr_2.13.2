#!/bin/bash
if [ "$pgservice_core" == "" ]; then
    # load environement variable for freedom
    . /etc/freedom.conf
    wchoose -b
fi

echo "ALTER TABLE permission ADD COLUMN computed boolean DEFAULT FALSE;" | PGSERVICE="$pgservice_core" psql
