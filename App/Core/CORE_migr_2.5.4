#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi


echo "CREATE UNIQUE INDEX uni_users on users (login,iddomain);" | psql $dbpsql

