#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi

echo "update users set firstname='' where  isgroup = 'Y';update users set isgroup = 'N' where isgroup is null;" | psql $dbpsql

echo "ALTER TABLE fld DROP COLUMN id;" | psql $dbfree
