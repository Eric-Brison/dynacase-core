#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi

echo "alter table doc120 add column us_pphone text;" | psql $dbfree
echo "update doc120 set us_pphone=us_phone;" | psql $dbfree


