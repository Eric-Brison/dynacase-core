#!/bin/bash
if [ "$freedom_context" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi

PGSERVICE="$pgservice_freedom" psql -c "ALTER TABLE docread ALTER COLUMN postitid TYPE text;"
PGSERVICE="$pgservice_freedom" psql -c "ALTER TABLE doc ALTER COLUMN postitid TYPE text;"
