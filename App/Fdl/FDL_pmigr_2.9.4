#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi


echo "update paramv set val=(select val from paramv where type='G' and name='CORE_URLINDEX') where name='TE_URLINDEX' and type='G';" | psql $dbpsql

