#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi
echo "update doc2 set cvid=(SELECT ccvid from docfam where id=2) where cvid is null;" | psql $dbfree

