#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi

echo "alter table doc add column allocated int;ALTER TABLE doc ALTER allocated set default 0;" | psql $dbfree