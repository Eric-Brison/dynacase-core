#!/bin/bash
if [ "$dbpsql" == "" ]; then
    #load environement variable for freedom
  . /etc/freedom.conf
   wchoose -b
fi


echo "UPDATE doc2 set fld_subfam = regexp_replace(fld_famids, '[0-9]+', 'yes','g');" | psql $dbfree

